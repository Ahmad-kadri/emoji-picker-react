import * as React from 'react';
declare type Props = Readonly<{
    children: React.ReactNode;
    className?: string;
}>;
export default function Absolute({ children, className }: Props): JSX.Element;
export {};
