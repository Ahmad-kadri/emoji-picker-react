/// <reference types="react" />
/**
 * Renders a nonce-tagged <style> element for dynamic CSS values
 * that cannot be expressed as static CSS classes (CSP-compliant).
 */
export declare function DynamicPickerStyle({ css }: {
    css: string;
}): JSX.Element;
